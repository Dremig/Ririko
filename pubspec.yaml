name: ririko
description: Record the bill
version: 0.0.0-beta
environment:
  sdk: '^3.10.0'

dependencies:
  flutter:
    sdk: flutter
  notification_listener_service: ^0.3.5
  sqflite: ^2.4.2
  path: ^1.9.0
  intl: ^0.19.0
  excel: ^4.0.6
  share_plus: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
